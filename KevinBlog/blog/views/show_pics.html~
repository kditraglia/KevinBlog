	{% if pics %}
	    {% for pic in pics %}
	    <h2><img src="{{STATIC_URL}}{{ pic.app_logo }}">{{ pic.name }}</img></h2>
	    <hr>
	    {% endfor %}
	{% else %}
	<p>There are no pics.</p>
	{% endif %}
